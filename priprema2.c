#include <stdio.h>
#include <ctype.h>
#define MAX 30

main() {
	/*int c, nc=0;
	while ((c=getchar()) != EOF)
		if(c==' ' || c=='\t')
			putchar('\n');
		else
			putchar(c);*/
	/*char ime[MAX];
	gets(ime);
	printf("\n Zdravo %s.", ime);*/

}